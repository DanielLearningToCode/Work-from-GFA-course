using System;
using System.Collections.Generic;
using System.Text;
using System.IO;
using System.Linq;

namespace test
{
    class WordFrequency
    {
        public static void SortWords(int frequency)
        {
  





           /* var wordSeparator = new char[] { ' ', '\r', '\n' };
            var output = String.Empty;
            var inputWords = File.ReadAllText("input.txt").Split(wordSeparator, StringSplitOptions.RemoveEmptyEntries).ToList();
            foreach (var item in inputWords)
            {
                int counter = 0;
                if (item.Count() >= frequency)
                {
                    
                }
                item.Count();
            }                      */  


        }
    }
}

/// 1. skeleton
/// 2. create var result
/// 3. read file
/// 4. parse data
/// 5. process words
/// 6. save the word if it appears with the frequency
/// 7. void, save words to file output.txt
///

///Write a method which can read and parse a file.
///The method must take a frequency as a parameter 
///and collects all of the words that appear in that 
///amount of times in the source file. It shall write
///the selected words into output.txt.
///If there is no word present with the given frequency it shall not do anything.